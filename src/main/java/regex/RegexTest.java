package regex;

import java.util.regex.Pattern;

public class RegexTest {
    private static final Pattern pattern = Pattern.compile("[0-9]+", Pattern.MULTILINE | Pattern.DOTALL);

    public static void main(String[] args) {
        
    }
}
